# 《文字冒险屋》v0.3.1 源码部署与游戏大厅接入说明

## 1. 交付内容

本压缩包是完整 Unity 源码工程，不是 APK、AAB、UnityPackage 或可直接覆盖大厅工程的插件。源码包含《抓住未尽的余晖》《密电疑云》《暗黑房间》三个子游戏、无障碍语义层、音乐音效、存档、自动化测试和大厅返回桥。

压缩包由 Git 标签 `v0.3.1` 的已跟踪文件生成，不包含 `.git`、`Library`、`Temp`、`Logs`、`Builds`、`UserSettings`、IDE缓存或开发机测试结果。大厅团队应在独立分支完成接入，不要直接覆盖正式大厅工程。

## 2. 开发环境

- Unity：`6000.3.21f1`，以 `ProjectSettings/ProjectVersion.txt` 为准。
- Android构建模块：Android Build Support、SDK、NDK、OpenJDK。
- 无障碍流程预览器为可选工具，需要 .NET 8 Windows Desktop Runtime/SDK；大厅接入和Android构建不依赖该预览器。
- 首次打开工程需要网络恢复 `Packages/manifest.json` 中固定提交的 Ink Unity Integration，其余Unity包按 `Packages/packages-lock.json` 恢复。

解压到普通可写目录后，用指定Unity版本直接打开源码包根目录。不要从包内复制 `Library`，也不要沿用其他Unity版本生成的缓存。

## 3. 独立工程验证

1. 等待Unity完成包恢复、资源导入和脚本编译。
2. 确认 Build Settings 中启用并保持以下顺序：
   - `Assets/Scenes/LoadScene.unity`
   - `Assets/Scenes/MainMenu.unity`
   - `Assets/Scenes/GameScene.unity`
   - `Assets/Scenes/PauseMenu.unity`
   - `Assets/Scenes/FailMenu.unity`
3. 从 `LoadScene` 进入播放模式，确认目录显示三款游戏、帮助、参数设置和返回游戏大厅。
4. 在 Unity Test Runner 运行 PlayMode 测试程序集 `FactoryEscape.Accessibility.PlayModeTests`。v0.3.1交付前结果为31项通过、0项失败、0项跳过。
5. 如需独立Android验证，可执行编辑器方法 `FactoryEscapeAutomation.BuildAndroid`；输出位于 `Builds/Android/FactoryEscape.apk`。最终大厅包应使用大厅自己的统一构建、包名、签名和版本策略。

## 4. 合并到游戏大厅

### 4.1 合并边界

- 合并 `Assets` 中的源码、场景、资源和 `.meta` 文件；合并 `Packages/manifest.json` 与 `Packages/packages-lock.json` 中本模块需要的依赖。
- 不要用本工程的整个 `ProjectSettings` 覆盖大厅配置。大厅保留自己的 Android 包名、签名、启动场景、版本号、全局方向、图形和输入设置，只按需加入本模块场景及必要配置。
- 本工程是完整Unity项目，脚本主要位于全局命名空间，不是隔离的UPM包。合并前须检查大厅现有的 `GameManager`、`SaveManager`、`soundManager`、单例基类和场景名称是否冲突；发生冲突时由大厅分支统一改名，不得删减无障碍语义层。
- `FactoryEscapeAccessibility` 与 `SystemChineseFontProvider` 当前使用 `RuntimeInitializeOnLoadMethod` 自动创建常驻对象。源码并入长期运行的大厅后，大厅团队必须把它们纳入文字冒险屋模块生命周期，确认不会在大厅或其他游戏页面提前接管无障碍层或TMP默认字体。

### 4.2 进入和返回协议

大厅在加载文字冒险屋入口场景前连接返回处理器：

```csharp
private System.Action<string> factoryEscapeReturnHandler;

private void EnterFactoryEscape()
{
    factoryEscapeReturnHandler = OnFactoryEscapeReturn;
    GameLobbyReturnBridge.Connect(factoryEscapeReturnHandler);
    UnityEngine.SceneManagement.SceneManager.LoadSceneAsync(
        "LoadScene",
        UnityEngine.SceneManagement.LoadSceneMode.Additive);
}

private void OnFactoryEscapeReturn(string reason)
{
    GameLobbyReturnBridge.Disconnect(factoryEscapeReturnHandler);
    factoryEscapeReturnHandler = null;

    // 由大厅在这里卸载文字冒险屋场景和模块对象，并恢复大厅界面及焦点。
    // 当前返回原因固定为 factory_escape_main_menu。
}
```

`返回游戏大厅`只调用上述处理器，不调用 `Application.Quit`。大厅回调负责停止并卸载文字冒险屋场景、音频和常驻对象，恢复大厅页面，然后把无障碍焦点放回原来的游戏入口。大厅销毁或中断模块时也必须调用 `Disconnect`，避免遗留返回处理器和方向会话。

### 4.3 竖屏范围

文字冒险屋的 `GameManager` 启用后，通过 `GameLobbyReturnBridge.EnterGameSession()`保存大厅原方向并强制本游戏竖屏。正常返回、断开或停用时会恢复此前方向和自动旋转状态。

大厅不得把本项目的竖屏要求写成整个App的构建期全局锁定，也不要删除进入和退出会话调用。接入验收必须覆盖“大厅原方向 → 进入文字冒险屋竖屏 → 返回大厅恢复原方向”。

## 5. 存档、音频与无障碍要求

- 三款子游戏使用稳定且不同的 `gameId` 和存档名；大厅不得把存档统一成同一个键。选择目录焦点本身不能读取存档或启动剧情。
- 最终运行音乐、音效和预录音频保持工程内 OGG Vorbis 资源；不要转回MP3、WAV或运行时PCM方案。
- 保留真实无障碍语义、焦点顺序、控件角色、标准激活、系统返回和返回后的焦点恢复。屏幕文字或图片OCR不能替代语义文本。
- 本游戏运行期间保持竖屏；除进入本游戏的会话外，不向大厅或其他游戏发送全局竖屏请求。

## 6. 许可与分发边界

- 《密电疑云》来源和MIT许可保存在 `Assets/GameContent/TheIntercept`。
- 《暗黑房间》为非官方无障碍研究版本，MPL-2.0许可证位于 `Assets/Resources/ThirdParty/ADarkRoom/LICENSE.txt`，上游源码归档位于 `ThirdParty/ADarkRoom`。
- 《暗黑房间》当前只限公益、内部研究。不得把包含该子游戏的APK或源码包向组织外测试者公开分发、上传商店、收费、接广告或提供公开下载。若发布范围变化，必须先重新进行许可证、商标、音频资产和源码提供审计。
- 合并时不得删除许可证、来源归属、上游源码归档或界面中的“非官方”说明。

## 7. 大厅接入验收清单

- 大厅可进入“文字冒险屋”，目录固定显示三款游戏。
- 三款游戏都能开始、继续、保存并返回文字冒险屋。
- 帮助顶部有返回按钮，标题列表可进入正文，系统返回可用。
- 音乐、音效及各自开关有效，默认不被大厅错误静音。
- 读屏能按顺序获得全部可操作项，激活与返回不依赖视觉或OCR。
- 进入后只锁定文字冒险屋竖屏，返回大厅后恢复大厅原方向。
- 返回游戏大厅不退出App、不重启App，并把焦点恢复到大厅中的文字冒险屋入口。
- Android候选包至少抽取大厅入口、文字冒险屋目录和一个正文页面的真机画面，检查中文、字号、对比度、比例与焦点框。
